[3:42 م، 2026/8/25] TARA STORE: create table if not exists archive_data (
  id text primary key,
  data jsonb not null
);

create table if not exists archive_users (
  id text primary key,
  data jsonb not null
);

-- إعطاء صلاحيات القراءة والكتابة
alter table archive_data enable row level security;
alter table archive_users enable row level security;

create policy "Allow all data" on archive_data for all using (true) with check (true);
create policy "Allow all users" on archive_users for all using (true) with check (true);
[3:46 م، 2026/8/25] TARA STORE: https://kukcejoiaadzujpmxlao.supabase.co/rest/v1/
[3:51 م، 2026/8/25] TARA STORE: sb_publishable_WNh6UWXfRTV7jHeC5mhIpQ_w8gk_4Kl
[3:54 م، 2026/8/25] TARA STORE: <!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>أرشيف الشركات</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Noto+Kufi+Arabic:wght@500;700;900&family=Tajawal:wght@400;500;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
<!-- Supabase JS Library -->
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<style>
  :root{
    --ink:#1E2B27;
    --paper:#EDEAE2;
    --paper-raised:#FBFAF6;
    --brass:#A87C3F;
    --brass-dark:#8A6530;
    --brass-light:#D9BE8E;
    --line:#D8D2C4;
    --danger:#8B3A3A;
    --danger-bg:#F3E4E2;
    --muted:#7A7468;
    --shadow: 0 6px 18px rgba(30,43,39,0.10), 0 1px 2px rgba(30,43,39,0.08);
  }
  *{box-sizing:border-box;}
  body{
    margin:0;
    background:
      radial-gradient(1200px 600px at 15% -10%, #F3F0E6 0%, transparent 60%),
      var(--paper);
    color:var(--ink);
    font-family:'Tajawal',sans-serif;
    min-height:100vh;
  }
  .wrap{max-width:1100px;margin:0 auto;padding:36px 24px 80px;}

  header.top{
    display:flex;align-items:flex-end;justify-content:space-between;
    border-bottom:3px solid var(--ink);
    padding-bottom:18px;margin-bottom:8px;
    gap:16px;flex-wrap:wrap;
  }
  header.top h1{
    font-family:'Noto Kufi Arabic',sans-serif;
    font-weight:900;font-size:30px;margin:0;letter-spacing:0.2px;
  }
  .top-right{display:flex;align-items:center;gap:10px;flex-wrap:wrap;}
  .stamp{
    font-family:'JetBrains Mono',monospace;
    font-size:11px;color:var(--brass-dark);
    border:1.5px solid var(--brass);
    border-radius:3px;padding:5px 10px;
    background:var(--paper-raised);
    white-space:nowrap;
  }
  .who{
    font-size:12.5px;color:var(--muted);
    display:flex;align-items:center;gap:6px;
  }
  .who b{color:var(--ink);}
  .role-badge{
    font-family:'JetBrains Mono',monospace;font-size:10px;
    background:var(--brass-light);color:var(--brass-dark);
    padding:2px 7px;border-radius:20px;
  }
  .btn{
    font-family:'Tajawal',sans-serif;font-weight:700;font-size:12.5px;
    padding:8px 14px;border-radius:8px;border:1px solid var(--line);
    background:var(--paper-raised);color:var(--ink);cursor:pointer;
  }
  .btn:hover{background:#fff;border-color:var(--brass);}
  .btn.ghost{background:transparent;}
  .btn.primary{background:var(--ink);color:#F3F0E6;border-color:var(--ink);}
  .btn.primary:hover{background:#33463F;}
  .btn.danger{background:transparent;color:var(--danger);border-color:var(--danger-bg);}
  .btn.danger:hover{background:var(--danger-bg);}
  .btn:disabled{opacity:.5;cursor:not-allowed;}

  .sub{color:var(--muted);font-size:13.5px;margin:10px 0 26px;}

  /* ---- login / setup ---- */
  .center-screen{
    min-height:70vh;display:flex;align-items:center;justify-content:center;
  }
  .auth-card{
    background:var(--paper-raised);border:1px solid var(--line);
    border-radius:14px;box-shadow:var(--shadow);
    padding:34px 32px;width:100%;max-width:360px;
  }
  .auth-card h2{
    font-family:'Noto Kufi Arabic',sans-serif;font-size:19px;margin:0 0 4px;
  }
  .auth-card p.hint{color:var(--muted);font-size:12.5px;margin:0 0 22px;}
  .field{margin-bottom:14px;}
  .field label{display:block;font-size:12px;color:var(--muted);margin-bottom:5px;font-weight:700;}
  .field input, .field select{
    width:100%;padding:10px 12px;border-radius:8px;border:1px solid var(--line);
    background:#fff;color:var(--ink);font-family:'Tajawal',sans-serif;font-size:13.5px;
  }
  .field input:focus, .field select:focus{outline:none;border-color:var(--brass);}
  .auth-error{
    background:var(--danger-bg);color:var(--danger);font-size:12.5px;
    padding:8px 12px;border-radius:8px;margin-bottom:1…